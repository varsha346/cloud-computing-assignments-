1.the .pem file and your project should be in the same folder , delete node modules 

2.  scp -i blog.pem -r BlogApp ec2-user@ec2-3-87-45-96.compute-1.amazonaws.com:~

3.chmod 400 blog.pem

4.ssh -i blog.pem ec2-user@ec2-3-87-45-96.compute-1.amazonaws.com

1. ls  cd BlogApp
2. install postgres - sudo yum update -y      

sudo yum install postgresql15 postgresql15-server -y      

sudo /usr/bin/postgresql-15-setup initdb

sudo systemctl start postgresql

sudo systemctl enable postgresql

sudo systemctl status postgresql

sudo -i -u postgres

psql

ALTER USER postgres WITH PASSWORD 'yourpassword';

CREATE DATABASE blogdb;

\q

exit

sudo nano /var/lib/pgsql/data/postgresql.conf

listen_addresses = 'localhost’ → ‘*’

sudo nano /var/lib/pgsql/data/pg_hba.conf

host    all             all             0.0.0.0/0               md5

sudo systemctl restart postgresql

Go to EC2 → Security Group → Add:

```
Custom TCP | Port 5432 | 0.0.0.0/0
```

backend and db are on the same server so no change in .env → just keep the user password same as here declared and user as postgres

1. Node js installation- curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

source ~/.bashrc

nvm install 20

nvm use 20

nvm alias default 20

node -v
npm -v

1. cd backend → npm install →npm init -y→node server.js
2. cd ..
3. cd frontend → npm install
4. change api_base to  ec public ip:5000; also add in inboud rules → port 5000
5. npm run build

13.Install Nginx

sudo yum install nginx -y

sudo systemctl start nginx
sudo systemctl enable nginx

sudo cp -r dist/* /usr/share/nginx/html/

 sudo nano /etc/nginx/conf.d/default.conf → paste this 

server {
listen 80;
server_name _;

```
root /usr/share/nginx/html;
index index.html;

location / {
    try_files $uri /index.html;
}
```

}

sudo systemctl restart nginx

sudo systemctl status nginx

ls /usr/share/nginx/html/